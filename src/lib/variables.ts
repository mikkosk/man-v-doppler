export const MAX_HERZ = 900; // Since the doppler effect affects the frequency, let's not allow the player to go over 900Hz so there is room
export const MIN_HERZ = 100;
export const DOMAIN_MAX = 1000;
export const DOMAIN_MIN = 0;
export const INTERVAL_MS = 20;